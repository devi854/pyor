on:
  pull_request:
    branches:
      - main
    
jobs:
  deploy:
     runs-on: ubuntu-latest
     
     steps:
       - name: checkout
         uses: actions/checkout@v3
         
env:
  DOTNET_INSTALLER_DIR: "runner/dotnet"
